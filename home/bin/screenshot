#!/usr/bin/env bash
#
# partial screenshot script using maim

dir=~/pics/screenshots
name=$(printf '%(%Y-%m-%d-%H-%M-%S)T\n' '-1')

mkdir -p "$dir" || exit 1

case "$1" in
	-s) maim -us "$dir/$name.png" ;;
	*)  maim -u "$dir/$name.png" ;;
esac
