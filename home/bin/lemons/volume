vol() {
	while true; do
		percent=$(pamixer --get-volume)
		muted=$(pamixer --get-mute)

		case $muted in
			true) icon="$vol_muted_icon" ;;
			false)
				((step=percent/34))
				icon="${vol_icons[$step]}"
				;;
		esac

		echo "VOLUME$icon $percent%"

		sleep 5
	done
}

# vim: ft=sh
