# sourced by bar

bri() {
	while true; do
		per=$(< ~/.cache/bri)
		
		((step=per/34))	
		icon="${bri_icons[$step]}"

		if ((show_bri_per)); then
			echo "L$icon $per%"
		else	
			echo "L$icon"
		fi

		sleep 2
	done
}

# vim: ft=sh
