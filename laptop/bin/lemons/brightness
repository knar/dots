# sourced by bar

bri() {
	path=/sys/class/backlight/intel_backlight

	val=$(< "$path/brightness")
	max=$(< "$path/max_brightness")
	
	((percent=val*100/max))
	
	((step=percent/34))	
	icon="${bri_icons[$step]}"

	echo "BRIGHTNESS$icon $percent%"
}

# vim: ft=sh
